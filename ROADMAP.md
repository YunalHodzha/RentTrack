# Приложение за следене на наеми — Project Roadmap

> Работно име: **RentTrack** *(подлежи на промяна — избери име преди публикуване и провери дали е свободно в App Store / Google Play)*
> Версия на документа: 1.2
> *Changelog: 2026-06-11 — преструктурирани фази 4.6–7, финализиран модел на монетизация. (1.1 — добавена Фаза 4.5.)*
> Платформи: iOS + Android (един код)

---

## 1. Контекст и проблем

Дребните и индивидуалните собственици на имоти под наем рядко имат удобен инструмент за следене на наемите си. Повечето използват тетрадка, Excel или просто паметта си, което води до пропуснати плащания, забравени падежи и липса на ясна картина за дохода.

Съществуващите приложения на пазара са основно насочени към американския пазар, сложни са, скъпи и не отчитат местните особености (език, валута, начин на работа на българския хазяин). Това оставя реална, недостатъчно покрита ниша.

**Накратко:** просто, фокусирано мобилно приложение, което прави следенето на наеми лесно и спокойно — без излишна сложност.

---

## 2. Цел на проекта

**Дългосрочна визия:** сериозен продукт, публикуван в App Store и Google Play, който дребни собственици използват ежедневно за управление на имотите си.

**Стратегия на разработка (важно):**
1. Първо — работещо приложение **за лично ползване** (валидира, че решава реален проблем).
2. После — отваряне **за други потребители** (вход, облак, синхронизация).
3. Накрая — **публикуване и монетизация**.

Това разделение е съзнателно: не се строи „за всички" от първия ред код. Първо нещо, което работи за самия теб.

**Как изглежда успехът:**
- v1: ползвам приложението сам, вместо таблица/тетрадка.
- v2: 5–10 души извън мен го ползват редовно и дават обратна връзка.
- v3: публикувано в двата магазина с работещ абонаментен модел.

---

## 3. Целеви потребители

- Дребни собственици с 1–20 имота (апартаменти, гаражи, земя, офиси).
- Хора, които не искат сложен счетоводен софтуер, а просто ред и спокойствие.
- Първоначален фокус: български пазар (език, евро/лев, локални навици).

---

## 4. Обхват

### В обхвата на v1 (MVP) — базовото
- Добавяне на имоти от различен тип (апартамент, гараж, земя, друго).
- Статус на всеки имот: свободен / под наем.
- Данни за наемател: име, телефон, сума на наема, ден за плащане.
- Записване на плащания и история на плащанията.
- Просто табло за преглед (доход, събрано/дължимо, заетост).

### В обхвата на следващи версии
- Локални известия (напомняния за падеж, аларми за неплатено).
- Месечни и годишни справки + PDF разписки.
- Проследяване на депозити и разходи (нетен доход).
- Бележки и документи към имот/наемател.
- Вход, облак и синхронизация (многопотребителски режим).
- Изпращане на напомняния директно към наемателя.

### Извън обхвата (съзнателно НЕ правим в началото)
- **Реална обработка на онлайн плащания в приложението** — тежка регулация (PCI), такси и отговорност. В началото само *записваме* плащания, не ги *обработваме*.
- Пълна счетоводна / данъчна интеграция.
- Едновременно уеб + мобилно приложение.
- Сложна автоматизация (банкови извлечения, OCR на договори) — много по-късно, ако изобщо.

---

## 5. Технологичен стек

| Слой | Избор | Защо |
|---|---|---|
| Език | **TypeScript** | Хваща грешки рано; струва си за реален проект |
| Framework | **React Native + Expo** | Един код за iOS и Android; знанието ти по React се пренася директно |
| Навигация | **Expo Router** | Файлова навигация, проста и модерна |
| Локална база (v1) | **expo-sqlite** | Релационни данни локално, без сървър |
| ORM (по избор) | **Drizzle ORM** | По-чисти заявки върху SQLite; може и чист SQL |
| Състояние | **Zustand** или React Context | Леко управление на state |
| Известия | **expo-notifications** | Локални насрочени известия за падежи |
| Дати | **date-fns** или **day.js** | Работа с периоди и падежи |
| UI стил | **NativeWind** (Tailwind за RN) или React Native Paper | NativeWind е близко до уеб опита ти |
| Backend (по-късно) | **Supabase** | Postgres + вход + синхронизация, щедър безплатен план |
| Билд и публикуване | **EAS Build / EAS Submit** | Прави билдовете за двата магазина |
| Контрол на версиите | **Git + GitHub** | Задължително за реален проект |
| Среда / асистент | **VS Code + Claude Code** | Claude работи директно в кода на всяка фаза |

**Бележка за iOS:** за финален билд и публикуване в App Store ще ти трябва Mac **или** EAS Build (облачен билд), който заобикаля нуждата от собствен Mac.

---

## 6. Модел на данните

Сърцето на проекта. Три основни таблици + две по-късно. Наемателят се пази отделно от имота през „наемно правоотношение" (lease), за да остава история при смяна на наемателите.

### `properties` (имоти)
| Поле | Тип | Описание |
|---|---|---|
| id | UUID / int | Първичен ключ |
| type | text | apartment / garage / land / office / other |
| name | text | Етикет, напр. „Ап. 3, ул. Осма" |
| address | text | Адрес (по избор) |
| status | text | free / rented / unavailable |
| notes | text | Свободни бележки |
| created_at | datetime | |

### `tenants` (наематели)
| Поле | Тип | Описание |
|---|---|---|
| id | UUID / int | Първичен ключ |
| name | text | Име |
| phone | text | Телефон |
| email | text | По избор |
| notes | text | Бележки |
| created_at | datetime | |

### `leases` (наемни правоотношения)
| Поле | Тип | Описание |
|---|---|---|
| id | UUID / int | Първичен ключ |
| property_id | FK → properties | Кой имот |
| tenant_id | FK → tenants | Кой наемател |
| rent_amount | number | Сума на наема |
| currency | text | EUR (основна) / BGN |
| payment_day | int (1–31) | Ден за плащане в месеца |
| start_date | date | Начало |
| end_date | date / null | Край (null = активен) |
| deposit_amount | number | Депозит |
| status | text | active / ended |
| notes | text | |

### `payments` (плащания)
| Поле | Тип | Описание |
|---|---|---|
| id | UUID / int | Първичен ключ |
| lease_id | FK → leases | Кое правоотношение |
| period | text (YYYY-MM) | За кой месец е плащането |
| amount | number | Платена сума |
| paid_date | date | Дата на плащане |
| status | text | paid / partial / pending |
| method | text | в брой / банка / друго |
| notes | text | |

### Бъдещи таблици
- `expenses` — разходи по имот (данъци, ремонти, поддръжка) → нетен доход.
- `documents` — снимки/файлове към имот или наемател (договор, електромер). **Внимание: GDPR при чужди лични документи.**

### Изчисляеми (derived) стойности — не се пазят като полета
- **Заетост:** брой имоти със статус `rented` спрямо общия брой.
- **Дължимо за месец:** активни leases без `paid` плащане за текущия период.
- **Неплатено / просрочено:** ако периодът мине след `payment_day` без записано плащане.

---

## 7. Архитектура

**v1 (local-first):** всичко на телефона в SQLite. Без сървър, без интернет, без регистрация. Бързо за разработка и напълно достатъчно за лично ползване.

**v2 (cloud):** добавя се Supabase — вход с email/парола, данните на всеки потребител отделени (row-level security), синхронизация между устройства. Локалната база остава като кеш за работа офлайн.

**Примерна структура на папките (Expo Router):**
```
/app            # екрани и навигация (файлова)
  /(tabs)       # табло, имоти, наематели, настройки
  /property/[id]
  /tenant/[id]
/components     # преизползваеми UI компоненти
/db             # схема, миграции, заявки
/lib            # помощни функции (дати, валута, изчисления)
/hooks          # custom hooks
/store          # състояние (Zustand)
/notifications  # логика за известия
```

---

## 8. Логика на известията

- За всеки активен lease се насрочва локално известие **N дни преди** `payment_day` всеки месец („Наемът за [имот] е до [дата]").
- При отваряне на приложението (или ежедневна фонова проверка) се проверява всеки активен lease: ако текущата дата е минала `payment_day` и няма `paid` плащане за периода → маркира се като просрочено и се показва аларма/значка.
- Известията са **локални** (без нужда от сървър) — затова работят и в v1.

---

## 9. Локална особеност: валута (евро / лев)

България е във валутния съюз — от 1 януари 2026 г. еврото е официалната валута (фиксиран курс 1 EUR = 1.95583 BGN). Двойното обозначаване на цени в лева и евро е задължително до 8 август 2026 г.

**Извод за приложението:** гради на **евро като основна валута**, но добави опция за паралелно показване в лева през преходния период. Дребна функция, която показва, че приложението е направено за българския пазар.

---

## 10. Етапи (Roadmap по фази)

> Без твърди срокове — темпото зависи от свободното време. Подредбата обаче е важна: всяка фаза стъпва на предишната.

### Фаза 0 — Подготовка
- Инсталиране на Node.js, Expo CLI; създаване на проект (`npx create-expo-app`).
- Git репо в GitHub; настройка на Claude Code във VS Code.
- Финализиране на модела на данните (този документ).

### Фаза 1 — MVP (само за теб, локално)
- CRUD за имоти, наематели, leases.
- Записване на плащания + история.
- Просто табло (доход, събрано/дължимо, заетост).
- Съхранение в `expo-sqlite`.
- **Резултат:** работещо приложение, което ползваш сам.

### Фаза 2 — Известия и справки
- Локални известия (падежи и просрочени).
- Месечни/годишни справки по имот и общо.
- PDF разписки за наемателите.

### Фаза 3 — Полиране
- Икона, имена, настройки, UX детайли.
- Валута EUR/BGN (двойно показване).
- Архив/експорт на данни; обработка на крайни случаи.

### Фаза 4 — Многопотребителски режим + облак
- Supabase: вход, отделени данни, синхронизация.
- **Резултат:** приложението става „за други хора".

### Фаза 4.5 — Консолидация и качество ✅
Завършена: merge на cloud клона в `main`, единна toast/feedback система, skeleton states без трептене, тих фонов sync и фикс на startup freeze.

### Фаза 4.6 — Release hardening
> Подготовка за външни потребители — затваря известните остри ръбове, преди някой друг да докосне приложението.
- Фикс на `paymentDay = 31` edge case в логиката за просрочие/известия (къси месеци).
- Password reset поток (Supabase + deep link обратно към приложението).
- Изтриване на акаунт от приложението (Настройки → изтрива Supabase акаунта + всички данни) — задължително изискване на Apple за приложения с регистрация.
- Date picker (`@react-native-community/datetimepicker`) вместо текстов вход за дати.
- Import / restore на JSON експорт.
- Crash reporting със Sentry (Expo интеграция).
- Onboarding / умни empty states за нов потребител („Добави първия си имот →").
- Entitlement слой (подготовка за Фаза 6): `getEntitlement(): 'free' | 'pro'` + `canAddProperty(count)`, които засега винаги връщат pro/true; всички места, създаващи имот, минават през тях.
- Пренаписване на README.md.

### Фаза 5A — Правно и идентичност
- Финално име на приложението + проверка за наличност в App Store / Google Play (RentTrack вероятно е заето).
- Икона 1024×1024, splash screen.
- Декларация за поверителност + условия за ползване, хоствани на публичен URL (GitHub Pages).
- GDPR: изтриване на данни при изтриване на акаунт, минимизация; App Privacy въпросник на Apple.

### Фаза 5B — Билд инфраструктура
- `app.json`: `ios.bundleIdentifier`, `android.package`, `versionCode` / `buildNumber`.
- `eas.json` с профили development / preview / production.
- Google Play Developer акаунт ($25 еднократно), Apple Developer Program ($99/год.).
- Първи EAS production билд + инсталация на реално устройство.

### Фаза 5C — Мек старт и публикуване
- TestFlight (iOS) + Internal testing track (Android) с 5–10 тестери.
- Екранни снимки, описание, ключови думи на български.
- 2–3 седмици обратна връзка → submit за ревю.
- Приложението се пуска напълно безплатно — монетизацията идва във Фаза 6.

### Фаза 6 — Монетизация
- Модел: freemium — безплатно до 2 имота, абонамент над това.
- Цени (ориентировъчно): 2,99–4,99 €/месец или ~30 €/година; опционален lifetime unlock (~60–80 €).
- Интеграция чрез RevenueCat (`react-native-purchases`); entitlement-ът от RevenueCat се връзва към `getEntitlement()` от Фаза 4.6.
- Grandfathering: потребители от мекия старт с повече имоти запазват пълен достъп до съществуващите си данни; лимитът важи само за добавяне на нови имоти.
- Записване в Small Business програмите на Apple и Google (комисиона 15% вместо 30%).

### Фаза 7 — Растеж
- Разходи по имот → нетен доход (`expenses` таблица).
- PDF разписки за наематели.
- Документи към имот/наемател (внимание: GDPR).
- Търсене / филтриране, swipe-to-delete, pull-to-refresh.
- Приоритизация според обратната връзка от реални потребители.

---

## 11. Публикуване — какво е нужно

- **Google Play Developer акаунт** — $25, еднократно.
- **Apple Developer Program** — $99 на година.
- **Mac или EAS Build** — за iOS билд.
- **Икона** (1024×1024) и **екранни снимки** за двата магазина.
- **Описание, ключови думи, категория.**
- **Декларация за поверителност** (задължителна, особено при лични данни).
- Очаквай **процес на ревю** (по-строг при Apple).

---

## 12. Правни аспекти / GDPR

Приложението обработва лични данни (имена, телефони, плащания, евентуално документи на наематели). При отваряне за други потребители това става сериозно:
- Декларация за поверителност и условия за ползване.
- Ясно правило как се събират, пазят и трият данните.
- Минимизиране: събирай само каквото е нужно.
- Право на изтриване на данни при поискване.
- Внимавай особено със снимки на лични документи (Фаза „documents").

*(Това не е правен съвет — при реално публикуване си струва кратка консултация с юрист.)*

---

## 13. Монетизация (решено)

- **Модел: freemium** — безплатно до **2 имота**; платен абонамент над това.
- **Цени (ориентировъчно):** 2,99–4,99 €/месец или ~30 €/година; опционален **lifetime unlock** (~60–80 €).
- **Интеграция чрез RevenueCat** (`react-native-purchases`) — entitlement-ът от RevenueCat се връзва към `getEntitlement()` от Фаза 4.6, така че кодът на приложението не знае нищо за магазините.
- **Grandfathering:** потребители от мекия старт (Фаза 5C) с повече от 2 имота запазват пълен достъп до съществуващите си данни; лимитът важи само за добавяне на нови имоти.
- **Пускането във Фаза 5 е напълно безплатно** — абонаментът се включва чак във Фаза 6, след реални потребители и обратна връзка.
- **Small Business програмите** на Apple и Google свалят комисионата от 30% на 15% — записване веднага щом има приходи.

**Отхвърлени варианти и защо:**
- **Реклами** — приложение за наеми се отваря няколко пъти месечно; при такава ниска честота на ползване ad приходът е нищожен, а рекламите рушат доверието в инструмент за лични финанси.
- **Еднократна покупка** — облакът (Supabase) и Apple Developer Program ($99/год.) са постоянни разходи; еднократен приход срещу постоянни разходи не е устойчиво. Lifetime unlock остава само като опционална премиум опция на цена, която покрива години разходи.

---

## 14. Рискове и как да ги избегнем

| Риск | Мярка |
|---|---|
| **Раздуване на обхвата** (най-големият) | Желязна дисциплина: пусни Фаза 1, преди да мислиш за останалото |
| Прегаряне от опит да направиш всичко наведнъж | Малки, завършени стъпки; всяка фаза дава работещ резултат |
| iOS изисква Mac | Планирай EAS Build отрано |
| GDPR подценен | Вземи го насериозно от Фаза 4–5, не накрая |
| Загуба на данни на потребителя | Архив/експорт от Фаза 3; внимателни миграции на базата |

---

## 15. Първи конкретни стъпки

1. Избери (поне работно) име на приложението.
2. Инсталирай Node.js и създай Expo проект с TypeScript.
3. Качи го в GitHub; пусни Claude Code в проекта.
4. Заложи схемата на базата от секция 6.
5. Започни Фаза 1: екран „Имоти" (списък + добавяне), после наематели и плащания.

---

*Този документ е жив — обновявай го с напредването на проекта.*
