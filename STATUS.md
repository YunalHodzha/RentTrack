# RentTrack — Статус на проекта

> Последна актуализация: 2026-06-08

---

## Свършено

### Основа (Phase 0)
- [x] Expo проект с TypeScript, Expo Router, NativeWind
- [x] SQLite база данни чрез `expo-sqlite` + Drizzle ORM
- [x] Пълна схема на базата: `properties`, `tenants`, `leases`, `payments`
- [x] Zustand store за глобално state управление
- [x] Инициализация на базата при стартиране на приложението

### Навигация и layout
- [x] Таб навигация: Табло / Имоти / Наематели
- [x] Stack навигация за детайлни екрани
- [x] Автоматично презареждане на данните при връщане към таб екран (`useFocusEffect`)
- [x] Dark / light mode поддръжка навсякъде

### Табло (`/`)
- [x] Месечен доход от всички активни договори
- [x] Заетост (брой наети / общо имоти)
- [x] Събрано и дължимо за текущия месец
- [x] Предупреждение за просрочени плащания
- [x] Начален екран при липса на данни

### Имоти (`/properties`)
- [x] Списък на всички имоти с тип и статус
- [x] Добавяне на имот (modal): име, адрес, тип, бележки
- [x] Навигация към детайлен екран при натискане

### Наематели (`/tenants`)
- [x] Списък на всички наематели
- [x] Добавяне на наемател (modal): име, телефон, имейл, бележки

### Детайлен екран на имот (`/property/[id]`) — ново
- [x] Преглед на данните на имота (тип, адрес, статус, бележки)
- [x] Редактиране на имота
- [x] Изтриване на имот (само ако няма активен договор)
- [x] Добавяне на договор за наем: наемател (избор от списък), сума, валута (EUR/BGN), ден за плащане, начална дата, депозит, бележки
- [x] Преглед на активен договор с всички детайли
- [x] Записване на плащане: период, сума, начин на плащане (в брой / банков превод / друго), бележки
- [x] Приключване на договор (с потвърждение)
- [x] Статус на плащане за текущия месец (платено / не е платено)
- [x] Пълна история на плащанията

### Локализация
- [x] Целият интерфейс на български език
- [x] Типове имоти на български (Апартамент, Гараж, Земя, Офис, Друго)
- [x] Статуси на български (Свободен, Под наем, Недостъпен)
- [x] Месеци / дати на български (напр. „юни 2026", „3 юни 2026")

### Детайлен екран на наемател (`/tenant/[id]`) — ново
- [x] Преглед на наемателя с аватар и контактна информация
- [x] Бутони за обаждане / имейл (директно от контактите)
- [x] Обобщение „Общо платено" по валута
- [x] Преглед на текущ договор с навигация към имота
- [x] Пълна история на плащанията (по всички договори)
- [x] Редактиране на наемател
- [x] Изтриване на наемател (с каскадно изтриване на история; блокирано при активен договор)
- [x] Наемателите в списъка вече са кликаеми

### Плащания и договори — ново
- [x] Редактиране и изтриване на плащане (натискане върху ред от историята)
- [x] Статус на плащане при записване (Платено / Частично / Очаква се)
- [x] Предплащане наведнъж за няколко месеца или цяла година („Брой месеци" с преглед на общата сума)
- [x] Валидация срещу дублиран период за един договор (вкл. при предплащане)
- [x] Смяна на статус на имот (Свободен ↔ Недостъпен) без договор
- [x] Навигация от договора към наемателя и обратно
- [x] Сумите се показват според валутата на договора (EUR €, BGN лв.)

### Дизайн система и визуално обновяване — ново
- [x] Централизирана тема (`src/theme`) — палитра, разстояния, радиуси, сенки; край на повтарящите се `isDark ? …` тернари
- [x] Преизползваеми компоненти (`src/components/ui.tsx`) — Header, Card, Badge, Button, Field/Input, ChipGroup, Avatar, FAB, SheetModal, EmptyState, ProgressBar и др.
- [x] Помощни функции за домейна (`src/lib/domain.ts`) — етикети, икони, форматиране на пари/периоди/дати
- [x] Модерно табло с акцентна карта за дохода, индикатор за заетост и подобрено предупреждение
- [x] Безопасни отстъпи (safe-area) вместо фиксиран paddingTop
- [x] Таб лентата зачита системната навигация на Android (край на застъпването с бутоните назад/начало)
- [x] Премахнат неизползваният boilerplate от Expo шаблона

---

## Phase 1 — завършена ✅

> Всички задачи по завършването на Phase 1 са изпълнени.

## Phase 2 — завършена ✅

> Всички задачи по завършването на Phase 2 са изпълнени.

## Phase 3 — завършена ✅

> Всички задачи по завършването на Phase 3 са изпълнени. Следваща цел: **Phase 4 — Облак и многопотребителски режим**.

### Висок приоритет
- [x] **Детайлен екран на наемател (`/tenant/[id]`)** — редактиране, изтриване, преглед на активен договор и история на плащанията
- [x] **Редактиране / изтриване на плащане** — вече се поддържа от историята на плащанията
- [x] **Валидация на уникалност на период** — не се допуска дублиране на плащане за същия период

### Среден приоритет
- [x] **Смяна на статус на имот** от детайлния екран (напр. Свободен → Недостъпен без договор)
- [x] **Частично плащане** — поддръжка за `status: 'partial'` при записване
- [x] **Навигация от договора към наемателя** — линк от детайлния екран на имота към наемателя

### Детайли на Phase 2
- [x] **Автоматично маркиране на просрочени плащания** — всички `pending` плащания за минали месеци/дати се маркират като `overdue` при стартиране на приложението
- [x] **Локални push известия** — напомяния 3 дни преди падежа на плащане; уведомления се преплануват при всяка промяна на плащане
- [x] **Месечни справки** — преглед на събрано/дължимо по месец и по имот за избран месец
- [x] **Годишни справки** — преглед на събрано/дължимо по месец за цяла година; обобщени статистики за годината

---

## Следващи фази (от ROADMAP)

### Phase 2 — Известия и справки ✅
- [x] Локални push известия (N дни преди падежа всеки месец)
- [x] Автоматично маркиране на просрочени плащания
- [x] Месечни и годишни справки по имот и общо
- [ ] PDF разписки за наемателите (отложено за Phase 3)

### Phase 3 — Полиране и UX ✅
- [x] ~~Двойно показване EUR / BGN~~ (пропуснато)
- [x] Потребителски настройки (валута по подразбиране, брой дни за напомняне)
- [ ] Икона, splash screen, име на приложението
- [x] Архив / експорт на данни (JSON или CSV)
- [ ] Обработка на крайни случаи (изтрит наемател с активен договор и др.)

### Детайли на Phase 3
- [x] **Нов раздел "Настройки"** — 4-та раб в таб навигацията
- [x] **Настройка на валута** — избор на предпочитана валута (EUR/BGN)
- [x] **Настройка на напомяне** — брой дни преди падежа за известие (0-30)
- [x] **Експорт на JSON** — експортира всички данни в JSON формат; възможност за споделяне
- [x] **Експорт на CSV** — експортира всички данни в CSV формат с множество таблици (имоти, наематели, договори, плащания)
- [x] **AsyncStorage интеграция** — запазване на настройки между сесии
- [x] **Динамично преплануване на известия** — известията се преплануват при изменение на настройките

### Phase 4 — Облак и многопотребителски режим

#### Phase 4A — Локални основи за синхронизация ✅ (без мрежа)
- [x] **`updatedAt` се обновява при всяка промяна** — `.$onUpdate()` на всичките 4 таблици в `schema.ts`, за да може sync двигателят да открива локално променени редове.
- [x] **Мек delete (soft delete)** — изтриванията на имот/наемател/плащане вече задават `deletedAt` + `updatedAt` вместо твърд `db.delete`. Каскадно подпечатване: имот/наемател → договори → плащания (`src/db/soft-delete.ts`). Твърдото изтриване остава само в `export.ts` (import-replace = пълен reset).
- [x] **Филтриране `isNull(deletedAt)`** добавено към всяка четяща заявка (екрани, services, store).
- [x] **`userId` колона** (nullable засега) на всичките 4 таблици — собственост на ред за бъдещата RLS изолация; pre-auth локалните редове ще бъдат „осиновени" при първи вход.
- [x] **Регенерирана миграция** — `drizzle/0001_pink_the_captain.sql` (ALTER TABLE ADD COLUMN user_id).
- [x] **Destructive dev-drop branch** в `initDatabase()` е заключен зад `__DEV__`, за да не може да трие реални данни в production билд.
- [x] **Тестове** за soft-delete каскада, филтрирането и `updatedAt`-on-update (`src/db/__tests__/soft-delete.test.ts`, in-memory better-sqlite3 + реалните миграции).

#### Phase 4B — Supabase backend ✅ (код готов; изисква конфигурация)
- [x] **Supabase схема** — `supabase/schema.sql`, огледало на `schema.ts` (uuid PK-та, TEXT timestamps за точен round-trip при LWW, `user_id` собственост, FK-и, partial unique).
- [x] **RLS** на всичките 4 таблици с `auth.uid() = user_id` (select/insert/update; без delete политика — само soft delete).
- [x] **`@supabase/supabase-js` клиент** — `src/services/supabase.ts`, чете `EXPO_PUBLIC_SUPABASE_URL` + `EXPO_PUBLIC_SUPABASE_ANON_KEY` от env; null когато не е конфигуриран; AsyncStorage сесия + auto-refresh.
- [x] **Auth store** — `src/store/auth.ts` (възстановяване на сесия, signUp/signIn/signOut, `onAuthStateChange`).
- [x] **Вход с имейл/парола + gating** — `src/components/auth-screen.tsx`; приложението е заключено зад вход в `_layout.tsx`; изход от „Настройки".
- [x] **Допълнителна корекция (от soft delete):** `unique(lease_id, period)` → partial unique index `WHERE deleted_at IS NULL` (миграция `0002`), за да може период да се преизползва след мек delete.
- [ ] **Изисква се от теб (не може да се автоматизира):** създай Supabase проект, пусни `supabase/schema.sql`, копирай `.env.example` → `.env` с твоите URL+anon key, и тествай вход на устройство/development build.
- [ ] Претендиране на pre-auth локалните редове — преместено в Phase 4C (item 9).

#### Phase 4C — Sync двигател ✅ (код готов)
- [x] **Ядро на синхронизацията** — `src/services/sync.ts`: pull→push цикъл с LWW по `updatedAt`; pull-first, за да не презаписва blind upsert по-нов отдалечен ред; echo-free (вече приложените от сървъра редове не се връщат обратно). Seams `SyncRemote` + `CursorStore` за тестваемост.
- [x] **Курсор** — единичен per-device high-water mark (`updatedAt`), namespaced по `userId` в AsyncStorage.
- [x] **Soft-delete през sync** — tombstone-ите се разпространяват като обикновени редове (bump-нат `updatedAt` + `deletedAt`).
- [x] **Claim** — при sync pre-auth локалните редове (`user_id IS NULL`) се присвояват на текущия потребител и се качват (item 11).
- [x] **Runtime + тригери** — `src/services/sync-runtime.ts`: Supabase remote (upsert/select), mutex-нат `syncNow`, тригери при вход, foreground (AppState), периодичен интервал (60s) и reconnect (NetInfo). Офлайн опашката е имплицитна (курсорът не мърда при провал).
- [x] **UI** — `src/store/sync.ts` (статус/version); таблата/списъците се презареждат при sync; раздел „Синхронизация" в Настройки с ръчен бутон + статус.
- [x] **Тестове** — `src/services/__tests__/sync.test.ts`: push, pull, LWW (по-нов печели/по-стар не презаписва), tombstone, claim, echo-free, two-device разпространение (in-memory better-sqlite3 + fake remote).
- [ ] **Изисква се от теб:** тествай реална синхронизация между две устройства/сесии след вход (кодът не може да се тества срещу жив Supabase без креденшъли в CI).

> Забележка за clock skew: реконсилирането се доверява на часовниците на устройствата (LWW по `updatedAt`). Приемливо за личен app на NTP-синхронизирани телефони; при нужда — server-sequence курсор.

### Фаза 4.5 — Консолидация, UX и подготовка за публикуване (в ход)

#### 4.5.1 — Консолидация на облака
- [x] **Слят `phase-4-cloud-sync` в `main`** — fast-forward, без конфликти; Phase 4 кодът (Supabase, auth, sync) вече е в `main`.
- [ ] **Изисква се от теб:** създай Supabase проект, пусни `supabase/schema.sql`, попълни `.env` с реални URL+anon key (изглежда вече има локален `.env`), и тествай реална синхронизация между две устройства/сесии (вкл. claim на pre-auth данни).

#### 4.5.2 — Поправки и хигиена ✅
- [x] **Clamp на падежа в известията** — `schedulePaymentReminders` вече ползва нова чиста `paymentDueDate(period, paymentDay)` в `domain.ts` (`Math.min(paymentDay, daysInMonth(...))`), за да не се „търкулва" денят (напр. 31 февруари). + unit тестове.
- [x] **Детерминиран flaky тест** — `partial unique (lease_id, period)` сменен на синхронен `expect(() => …run()).toThrow()` (better-sqlite3 е синхронен; `await expect(builder).rejects` беше нестабилен).
- [x] **AGENTS.md** изравнен към Expo SDK 54 (v54.0.0 docs вместо v56).
- [x] **Тулинг хигиена** — `@types/jest` изравнен на 29.5.14; `npx expo-doctor` 18/18, `expo install --check` чист.
- [x] **Зелени тестове и lint** — 56 теста минават, `expo lint` 0 errors (11 заварени warning-а по UX екраните, за 4.5.3).

#### 4.5.3 — UX полиране ✅

**Част 1 — единна система за обратна връзка (toast) ✅**
- [x] **Toast инфраструктура** — `src/store/toast.ts` (Zustand + императивен `toast.success/error/info`) и `ToastHost` в дизайн системата (`ui.tsx`): safe-area, dark/light, fade+slide, авто-скриване, ръчно затваряне, стек до 3. Монтиран веднъж в `_layout.tsx`.
- [x] **Закачен за write действията** — имот/наемател (добавяне/редакция/изтриване + блокирано при активен договор), договор (създаване/приключване), плащане (запис/редакция/изтриване), вход/изход, ръчен sync. Тихите провали в базата вече дават грешен toast.
- [x] **Заварените 11 lint warning-а изчистени** (exhaustive-deps → нов `use-focus-reload` hook; неизползвани импорти в `reports.tsx`; `Array<T>`→`T[]`).
- Бележки: toast не се показва над отворен RN `Modal`, затова резултатът се показва след затваряне на модала, а валидацията в модал (вкл. „дублиран период") остава inline — вграждането ѝ е следваща част „Форми". Фоновите sync грешки мълчат (само ръчният sync известява).

**Част 2 — състояния на екраните (loading / empty / error) ✅**
- [x] **Нови компоненти** — `Skeleton`/`ListSkeleton` и `ErrorState` („Опитай отново") в дизайн системата; hook `use-loading-state` (`useDelayedFlag` + `useLoadingState`).
- [x] **Без трептене** — skeleton се показва само ако заетостта трае >~220ms; моменталните локални четения минават през „pending" (нищо за един кадър), без проблясък на „все още няма…". На практика skeleton се вижда само при първоначалния облачен pull.
- [x] **Loading** — табло, списъци имоти/наематели (skeleton), детайл имот/наемател (delayed спинер, без флаш), справки (skeleton при генериране).
- [x] **Empty** — списъчните „все още няма…" с бутон за добавяне; празните секции в детайлите вече съществуваха.
- [x] **Error** — провал на старта (`_layout`: `ErrorState` + „Опитай отново" вместо безкраен спинер), провал на четене по екраните (`ErrorState` + retry); провал на sync → toast (старт известява при грешка; не дублираме на екран).

**Част 3 — pull-to-refresh + търсене/филтриране ✅**
- [x] **Pull-to-refresh** на списъците имоти/наематели — дръпването пуска реален `syncNow` (mutex-нат), после reload; резултат през toast-а от Част 1; без Supabase — само локален reload.
- [x] **Търсене/филтриране** (клиентско, моментално, без дебаунс) — имоти: текст по име/адрес + ChipGroup филтри по статус и тип; наематели: текст по име/телефон.
- [x] **„Няма съвпадения"** (🔍) при празен резултат от търсене — различно от „все още няма…"; лентата за търсене се показва само при наличие на данни.

**UX поправки от тест на устройство (Android) — преди swipe-to-delete ✅**
- [x] **ConfirmDialog (единен стил)** — `src/store/confirm.ts` (Zustand + императивен `confirm()` → `Promise<boolean>`, по модела на toast-а) и `ConfirmHost` в дизайн системата: същински transparent RN `Modal` (показва се НАД отворен `SheetModal`), центрирана карта с цветове/типография/радиуси/safe-area от темата (dark/light), разрушителното действие в `danger` тон. Монтиран веднъж в `_layout.tsx`. Заменени всички разрушителни потвърждения (изход, възстановяване на данни, изтриване на имот/наемател/плащане, приключване на договор); еднобутонните валидационни Alert-и остават за частта „Форми".
- [x] **Клавиатура не крие полето за парола (Android)** — `KeyboardAvoidingView` `behavior` = `padding` (iOS) / `height` (Android); при отворена клавиатура auth формата се подравнява отгоре вместо центрирана; `app.json` → `android.softwareKeyboardLayoutMode: "resize"`. Същият `behavior` фикс приложен и на споделения `SheetModal` (формите договор/плащане).
**Част 4 — swipe-to-delete на списъците ✅**
- [x] **`SwipeableRow` в дизайн системата** (`ui.tsx`) — върху `ReanimatedSwipeable` (`react-native-gesture-handler/ReanimatedSwipeable`, Reanimated версията; старият `Swipeable` е deprecated). Плъзване наляво разкрива червено „Изтрий" (`danger` от темата, `accessibilityRole="button"`); при изтриване редът се свива навън (височина+отстояние+прозрачност, Reanimated `withTiming`), при отказ Swipeable се затваря обратно. `GestureHandlerRootView` вече беше в `_layout.tsx`.
- [x] **Закачен за имоти и наематели** (`(tabs)/properties.tsx`, `(tabs)/tenants.tsx`) — минава през СЪЩИЯ `confirm()` диалог, СЪЩИЯ soft-delete + toast и СЪЩАТА блокировка „има активен договор" като детайлните екрани (проверката чете свежо от базата); при наемател с история — същото предупреждение, че договорите/плащанията също се трият. Работи и върху филтрирания списък; бутоните за изтриване в детайлните екрани остават (достъпност/алтернатива).
- [x] **Ясно предупреждение при изтриване с история** — общ текст `deleteCascadeWarning(name)` в `domain.ts` („Това ще изтрие и всички договори и плащания към „<име>“. Историята на плащанията повече няма да се вижда в справките и събраните суми. Действието е необратимо."), показван в `confirm()` на 4-те места (детайл + swipe, имот + наемател) само когато има поне един жив договор (плащанията висят от договорите); без история остава краткият въпрос. Имотният детайлен екран вече също проверява за история (дотогава нямаше предупреждение). Soft-delete логиката и отчетите не са пипани.
**Част 5 — форми: date picker + клавиатура ✅** (изисква проверка на устройство — нов нативен модул)
- [x] **`@react-native-community/datetimepicker` 8.4.4** (през `npx expo install`, SDK 54; config plugin добавен в `app.json` — нужен е нов dev build / EAS build, в Expo Go модулът е наличен).
- [x] **`DateField` в дизайн системата** — стойност `yyyy-MM-dd` (формата в базата), показва се през `formatDate`; Android: нативният диалог (следва системната тема — платформата не позволява форсиране), iOS: spinner в долен лист с `themeVariant` от темата, commit на „Готово"/отказ при tap на фона; по избор ✕ за изчистване (`onClear`); затваря клавиатурата при отваряне.
- [x] **`MonthField` в дизайн системата** — лек месец/година селектор (‹ година › + решетка 12 месеца, стил ChipGroup) за период `yyyy-MM`; нативният picker няма чист „само месец" режим кросплатформено.
- [x] **Заменени полета** (`property/[id].tsx`): начална дата на договор → DateField; **нова** крайна дата (по избор, ✕ за изчистване; справките вече я ползват за очакваните периоди; валидация крайна ≥ начална); период на плащане → MonthField (add + edit); **нова** „Дата на плащане" (DateField, само при статус „платено"; дотогава винаги се записваше „днес" — поведението при останалите статуси е запазено). „Ден за плащане" (1–31) остава числово поле с валидация — не е дата.
- [x] **Клавиатура — проверено, без нужда от промени**: keyboardType коректни навсякъде (decimal-pad за наем/депозит/сума, number-pad за ден/брой месеци, phone-pad/email-address при наемател); SheetModal height/padding фиксът покрива и долните полета на договорната форма (хедър извън KAV, нативният ScrollView скролва фокусираното поле над клавиатурата); двете най-неудобни полета (датите с пълна текстова клавиатура) вече са picker-и. Съществуващата валидация (вкл. „дублиран период") е непипана.
**Част 6 — полева валидация във формите ✅** (последно парче)
- [x] **`Field`/`Input` с `error`** в дизайн системата (`ui.tsx`) — `Field` получи `error?: string` (съобщение в `danger` цвят под полето, с предимство пред `hint`, `accessibilityRole="alert"`), `Input` — `error?: boolean` (червен ръб). Цветовете от темата (dark/light).
- [x] **Заменени всички еднобутонни валидационни `Alert.alert` с грешки под полето** — договор (наемател, сума, ден 1–31, начална дата, крайна ≥ начална, име на inline нов наемател), плащане (сума, период, брой месеци 1–36, „дублиран период" под полето за период/MonthField — вкл. изброяване при няколко конфликтни месеца), добавяне/редакция на имот и наемател (име). Правилата на валидацията са непипани — само представянето.
- [x] **Поведение** — грешките се показват при submit (формата не се подава, докато има грешки; всички наведнъж, не само първата); грешката на поле се чисти при редакцията му (вкл. зависимите: начална дата чисти и крайната, брой месеци чисти и „дублиран период"). Кратки български съобщения („Изберете наемател", „Въведете валидна сума", „Денят трябва да е между 1 и 31", „Вече има плащане за този период").
- [x] **Никакви `Alert.alert` в кода** (grep чист) — потвържденията минават през `ConfirmDialog`, резултатите през toast, валидацията през полеви грешки. Провалът при inline създаване на наемател също е inline (toast не се вижда над отворен `SheetModal`).
- Отпаднали от обхвата на 4.5.3 (по решение — при нужда като отделно полиране): haptics; disabled бутони при запис; разширен одит на достъпност.

### Phase 5 — Публикуване
- [ ] Google Play Developer акаунт ($25)
- [ ] Apple Developer Program ($99/год.) + EAS Build за iOS
- [ ] Декларация за поверителност и условия за ползване (GDPR)
- [ ] Материали за магазините (икона 1024×1024, екранни снимки, описание)

---

## Предложения и бележки

### Подобрения на UX (краткосрочно)
- **Date picker** вместо text input за начална дата — `@react-native-community/datetimepicker` е включен в Expo и ще подобри значително usability-то
- **Swipe-to-delete** на редове в списъците — по-естествено от навигация в детайлен екран само за изтриване
- **Pull-to-refresh** на списъците с имоти и наематели
- **Търсене / филтриране** на имоти по статус и тип

### Технически бележки
- Схемата е готова за Phase 4 — `leases.currency` вече поддържа EUR/BGN; нужно е само UI за показване в лева
- При добавяне на Supabase в Phase 4, `id` полетата трябва да се сменят от `int autoincrement` на UUID, за да работи синхронизацията правилно — планирай тази миграция навреме
- `expo-sqlite` работи само на устройство/емулатор, не в уеб (Expo Go web) — нормално за мобилно приложение
- Предплащането създава по един „платено" запис за всеки покрит месец. Така бъдещата логика за известия (Phase 2) трябва само да проверява дали има платен запис за месеца — напомняния няма да се пращат за вече предплатените месеци до края на периода

### Тествай сега
Приложението е готово за реална употреба за лично ползване. Препоръчително е да се тества с реални данни преди да се добавят нови функции — ще изпъкнат edge cases, които са по-лесни за оправяне сега.
